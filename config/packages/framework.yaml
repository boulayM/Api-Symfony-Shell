framework:
  secret: '%env(APP_SECRET)%'
  http_method_override: false
  handle_all_throwables: true
  session: false
  csrf_protection: false
  serializer:
    enabled: true
  validation:
    enabled: true